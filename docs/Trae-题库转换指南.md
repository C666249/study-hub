# Trae 题库转换操作指南

> 目标：把 docx 格式的法学作业题库 → 结构化 JSON，导入 `study-hub/data/` 目录。

---

## 一、项目背景

`study-hub` 是一个通用刷题学习系统（单 HTML + localStorage + Netlify 部署）。当前只含刑法 10 道题作为测试数据。

你的任务：**批量将 docx 题库转为 JSON，补充解析，放入对应目录。**

工作目录：`d:\Claude\projects\study-hub\`

---

## 二、项目结构

```
study-hub/
├── index.html              ← 主应用（单文件，所有代码在此）
├── data/                   ← ★ 题库 JSON 放这里
│   └── 刑法.json            ← 示例（作业二的 10 题，可以参考格式）
├── src/core.js             ← 核心逻辑（测试用，不用管）
├── tests/core.test.js      ← 测试（不用管）
└── docs/
    └── Trae-题库转换指南.md  ← 本文档
```

---

## 三、JSON 数据格式

每题一个对象，所有题组成数组。**每个字段都是必需的。**

### 3.1 判断题 (type: "judge")

```json
{
  "id": "hw1_judge_1",
  "type": "judge",
  "homework": 1,
  "tags": ["刑法的效力"],
  "question": "外国人在中国领域内犯罪的，都适用我国刑法。",
  "options": null,
  "answer": "错",
  "explanation": "根据《刑法》第11条，享有外交特权和豁免权的外国人犯罪通过外交途径解决，并非一律适用我国刑法。"
}
```

### 3.2 单选题 (type: "single")

```json
{
  "id": "hw2_single_1",
  "type": "single",
  "homework": 2,
  "tags": ["危害行为"],
  "question": "下列哪一选项构成不作为犯罪？",
  "options": {
    "A": "丁邀秦某到风景区漂流…丁未下河救人，秦某溺亡",
    "B": "乙女拒绝周某求爱…乙未呼救，周某溺亡",
    "C": "甲到湖中游泳…甲未予理睬，武某溺亡",
    "D": "丙将不善游泳的贺某拉到深水区…贺某溺亡"
  },
  "answer": "D",
  "explanation": "A、B、C均无刑法上救助义务。D正确：丙的先行行为使其产生救助义务，能救而不救，成立不作为犯罪。"
}
```

### 3.3 多选题 (type: "multiple")

```json
{
  "id": "hw1_multiple_1",
  "type": "multiple",
  "homework": 1,
  "tags": ["刑法的效力"],
  "question": "关于刑事管辖权，下列哪些选项是正确的？",
  "options": {
    "A": "甲在国外教唆陈某到中国境内实施绑架行为，中国司法机关对甲的教唆犯罪有刑事管辖权",
    "B": "隶属于中国某边境城市旅游公司的长途汽车在从中国进入E国境内之后…",
    "C": "中国法院适用普遍管辖原则对劫持航空器的丙行使管辖权时…",
    "D": "外国人丁在中国领域外对中国公民犯罪的…也可能不适用中国刑法"
  },
  "answer": "ABD",
  "explanation": "A正确：属地管辖的扩展。B错误：长途汽车进入外国境内后不视为中国领土延伸。C错误：普遍管辖的定罪量刑依据是中国刑法而非国际条约。D正确：保护管辖有条件限制。"
}
```

### 3.4 简答题 (type: "short")

```json
{
  "id": "hw1_short_1",
  "type": "short",
  "homework": 1,
  "tags": ["刑法的效力"],
  "question": "简答我国刑法对空间效力作了哪些规定？",
  "options": null,
  "answer": "属地管辖原则、属人管辖原则、保护管辖原则、普遍管辖原则。",
  "explanation": "属地管辖（第6条）：凡在中国领域内犯罪的，除法律有特别规定外，都适用本法。属人管辖（第7条）：中国公民在中国领域外犯本法规定之罪的，适用本法。保护管辖（第8条）：外国人在中国领域外对中国国家或公民犯罪…普遍管辖（第9条）：对于中国缔结或参加的国际条约所规定的罪行…"
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | 唯一标识，格式：`hw{作业号}_{题型}_{序号}`，如 `hw2_single_3` |
| `type` | string | `"judge"` / `"single"` / `"multiple"` / `"short"` |
| `homework` | int | 作业编号（1, 2, 3…） |
| `tags` | string[] | 知识点标签，如 `["危害行为", "因果关系"]`。从 docx 末尾"知识点：XXX"提取 |
| `question` | string | 题干（纯文本，去掉题号和分值） |
| `options` | object\|null | 选项，key 为 A/B/C/D。判断题和简答题填 `null` |
| `answer` | string | **正确答案**。判断：`"对"`/`"错"`；单选：`"A"`~`"D"`；多选：`"ABD"`（字母升序）；简答：参考要点全文 |
| `explanation` | string | **解析**。如果 docx 里有"答案解析"段落就用它（整理措辞）；如果没有则根据知识点补充 |

---

## 四、docx 提取方法

### 4.1 原理

docx 本质是 ZIP 文件。解压后，内容在 `word/document.xml` 中。文本在 `<w:t>` 标签内。

### 4.2 提取命令（Bash）

```bash
# 1. 解压
cp "作业一.docx" /tmp/hw1.docx
cd /tmp && unzip -o hw1.docx -d hw1_content

# 2. 提取纯文本
cat hw1_content/word/document.xml \
  | sed 's/<[^>]*>//g' \
  | sed 's/&#[0-9]*;//g' \
  | tr -s ' \t\n' '\n'
```

### 4.3 解析注意事项

**区分正确答案和我的答案**：

docx 中可能包含两种答案标记：
- `正确答案：D` ← 用这个
- `我的答案：B` ← **忽略！**这是学生提交的答案
- `0分` / `1分` ← 得分标记，忽略

提取逻辑：
1. 如果有 `正确答案：X` → 直接用
2. 如果只有 `我的答案：X` 且没有 `正确答案` → 需要你自己判断对错，标记正确答案
3. 如果完全没有答案标记 → 多选题/单选题需要你根据法律知识判断

**从"答案解析"段提取知识点标签**：
- 作业二的 docx 每道题末尾有 `知识点：危害行为的分类`
- 提取这个作为 `tags` 值

---

## 五、解析字段

`explanation` 字段**统一留空字符串 `""`**。系统只显示 ✅❌ + 正确答案，不需要解析。

> 上轮 马原 已根据此规则清理完毕：196 条无效解析清空，保留 63 条 docx 自带的。

---

## 六、输出规范

### 6.1 文件命名

```
data/刑法.json    ← 所有刑法题目
data/马原.json    ← 所有马原题目
data/行政法.json   ← 所有行政法题目
```

**每个科目一个 JSON 文件，包含该科目所有作业的题目。** 不要按作业拆成多个文件。

### 6.2 JSON 格式要求

```json
[
  { "id": "hw1_judge_1", ... },
  { "id": "hw1_judge_2", ... },
  ...
]
```

- 顶层是数组 `[...]`
- UTF-8 编码（不要 BOM）
- 缩进 2 空格
- 不要尾逗号
- 所有字符串用双引号 `"`

### 6.3 中文字符处理

- 选项/题干中的中文引号 `""` **必须**替换为 `「」`，否则 JSON 解析会报错
- em-dash `——` 替换为 `——`
- 其他 Unicode 中文标点没问题

---

## 七、验证方法

### 7.1 JSON 语法验证

```bash
node -e "const q = require('./data/刑法.json'); console.log('OK:', q.length, 'questions')"
```

### 7.2 执行项目测试

```bash
cd d:/Claude/projects/study-hub
node tests/core.test.js
```

### 7.3 参考文件

已完成并验证的题库：[data/马原.json](file:///D:/Claude/projects/study-hub/data/马原.json)（259 题，结构规范，可直接参考格式）。

### 7.4 集成到 index.html

你只需输出 JSON 到 `data/` 目录。系统通过 tabs 切换 + `fetch('data/科目名.json')` 自动加载，无需改 HTML。

---

## 八、作业一已知信息（供参考）

作业一《刑法的效力》共 9 道题，题型分布见 docx。docx 中只有"我的答案"标注，无"正确答案"标注——需自行判断，不能用"我的答案"。

---

## 九、任务队列

当前待转换：
1. **刑法作业一**（9 题，`data/刑法.json`，追加到现有 10 题后面）  
2. **行政法**（如有 docx → `data/行政法.json`）
3. **民法**（如有 docx → `data/民法.json`）
4. **英语**（如有 docx → `data/英语.json`）

每完成一个科目，输出到 `data/科目名.json`，在本文档末尾追加一节报告（参考第十一节的格式）。

> 加新科目时告诉我，我在 `index.html` 的 `SUBJECT_LIST` 加一行即可上线。

---

## 十、常见陷阱

1. ❌ 把 `我的答案` 当成 `正确答案`
2. ❌ 多选题答案字母没排序（应该 `"ABD"` 不是 `"DAB"`）
3. ❌ 选项含中文引号 `""` → 必须替换为 `「」`
4. ❌ `tags` 为空数组或缺失
5. ❌ 判断题/简答题 `options` 不为 `null`
6. ❌ `explanation` 非空字符串（**统一填 `""`**）
7. ❌ 追加到已有 JSON 时忘记去重（按 `id` 检查）

---

## 十一、马原题库转换结果说明（2026-07-03）

### 11.1 任务完成情况

已按本指南要求把 `D:\edge download\学习\马原\学习通题库` 下的 5 个 docx 文件批量转换为结构化 JSON，输出到 [data/马原.json](file:///D:/Claude/projects/study-hub/data/马原.json)。

### 11.2 源文件与题目数量

| 源文件 | 声明题数 | 实际解析 | 对应作业号 |
|--------|---------|---------|-----------|
| 平时作业（一）.docx | 60 | 60 | homework=1 |
| 马克思主义哲学练习题二.docx | 50 | 50 | homework=2 |
| 马克思主义哲学练习三.docx | 50 | 50 | homework=3 |
| 马克思主义哲学练习五.docx | 49 | 49 | homework=5 |
| 马克思主义哲学练习六.docx | 50 | 50 | homework=6 |
| **合计** | **259** | **259** | — |

各文件解析数量与首页"共 X 道题目"声明完全一致，无丢题。

### 11.3 题型分布

| 题型 | 数量 | type 值 |
|------|------|---------|
| 单选题 | 178 | `single` |
| 多选题 | 64 | `multiple` |
| 判断题 | 17 | `judge` |
| **合计** | **259** | — |

> 本次源文件未出现简答题（`short`），马原题库暂无该类型。

### 11.4 数据质量校验

- ✅ 顶层为数组，UTF-8 无 BOM，2 空格缩进，无尾逗号
- ✅ 所有字段齐全（id / type / homework / tags / question / options / answer / explanation）
- ✅ 判断题 `options` 为 `null`
- ✅ 多选题答案字母已升序排序（如 `"ABCD"`）
- ✅ 中文引号 `""` 已统一替换为 `「」`
- ✅ `id` 命名规范：`hw{作业号}_{题型}_{序号}`
- ✅ `node tests/core.test.js` 6 项核心测试全部通过

### 11.5 解析过程要点（供后续参考）

1. **docx 提取**：docx 本质是 ZIP，解压后取 `word/document.xml`，用正则 `<[^>]+>` 剥离标签得到纯文本
2. **分块陷阱**：题号跨两位（如"10."）时，提取出的文本会把"1"和"0."拆到两行，导致正则 `^\d+\.\s*\([单多判]选题\)` 失配。已通过"数字缓冲行合并"修复，否则会丢掉 17 道判断题
3. **答案区分**：严格使用 `正确答案：X`，忽略 `我的答案：X` 和 `0分/2分` 得分标记
4. **选项两种格式**：
   - 格式 A：`A. 选项内容`（一行完整）
   - 格式 B：选项内容先单独成行列出，后面才出现 `A.` `B.` 空标记（如 hw1 第 3 题）。解析器已兼容：当未检测到带内容的 A. 时，按出现顺序把后续 4 行填入 A/B/C/D
5. **去重**：5 个文件之间无重复题，259 题全部保留

### 11.6 解析补充说明

源 docx 无"答案解析"段落，按指南第 5.2 节要求自行补充。本次采用"关键词匹配 + 通用兜底"策略：

- 判断题：按题干关键词（如"马克思主义仅仅是""完全继承""经济政治发展不平衡"等）给出针对性解析，无法匹配的用兜底文案"该表述符合/不符合马克思主义基本原理"
- 单选/多选题：按知识点分类（实践与认识、真理观、唯物辩证法、列宁主义、习近平新时代中国特色社会主义思想等）给出通用解析

**已知不足**：部分题目的 `explanation` 仍偏通用（如"根据马克思主义基本原理进行分析判断"），未做到逐选项剖析。如需更精细的解析，建议后续逐题人工补充，重点放在多选题的逐选项说明上。

### 11.7 待办（由项目维护者处理）

- [ ] 把 `index.html` 中的 `ALL_QUESTIONS` 数组替换为从 `data/马原.json` fetch 加载（指南第 7.3 节）
- [ ] （可选）人工精修解析文案，替换通用兜底内容
- [ ] （可选）补充 `tags` 知识点标签的粒度，当前部分题目统一归为"马克思主义基本原理"

### 11.8 交付文件

- 题库 JSON：[data/马原.json](file:///D:/Claude/projects/study-hub/data/马原.json)
- 转换脚本（临时）：`C:\Users\h'x'h\AppData\Local\Temp\convert_marx_v6.js`（如需复用可移入项目）
